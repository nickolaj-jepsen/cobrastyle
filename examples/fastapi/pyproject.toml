[project]
name = "example-fastapi"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["cobrastyle[cli,fastapi,jinja2]", "uvicorn>=0.30"]

[tool.uv]
package = false

[tool.uv.sources]
cobrastyle = { workspace = true }
